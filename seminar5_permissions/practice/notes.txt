su 
    требует права root, неполное переключение

su - 
    требует права root

sudo / sudo -i 
    требует права текущего пользователя
    проверяет, что пользователь или одна из его групп прописаны в файле /etc/sudoers

Время после выполнения sudo, во время которого можно не вводить пароль

find -uid
find -user


Основные и дополнительные группы
Удаление основной группы с тем же именем при удалении пользователя.


Задать сразу владельца и группу владельца
sudo chown alice:students 
либо использовать sudo chgrp


Не сработает:
sudo echo "Hello" > /etc/myfile
Сработает:
sudo sh -c 'echo "Hello" > /etc/myfile'
echo "Hello" | sudo tee /etc/myfile


Более подробно о командах оболочках  sh  и  bash
Запуск sh -c ''


Права
chmod u+rwx,g+rx,o+r a.txt
chmod u=rwx,g=rx,o=r a.txt


umask

Как меняются права при копировании/перемещении?


chmod 666 alpha/
ls alpha



Задача Вложенных директории

В каких случаях можно применять chmod, chown, chgrp

